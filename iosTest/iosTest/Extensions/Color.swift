//
//  Color.swift
//  iosTest
//
//  Created by Александра Згонникова on 24.07.2025.
//

import Foundation
import SwiftUI

extension Color {
    static let theme = ColorTheme()
}

struct ColorTheme {
    let accentColor = Color("accentColor")
    let colorWhite = Color("colorWhite")
    let colorBlack = Color("colorBlack")
    let colorForBorders = Color("colorForBorders")
    let colorForInputText = Color("colorForInputText")
    let background = Color("background")
    let accentColor40 = Color("accentColor40")
    let accessColor = Color("accessColor")
    let secondaryNavig = Color("secondaryNavig")
    let colorForSelector = Color("colorForSelector")
    let dividerColor = Color("dividerColor")
}
